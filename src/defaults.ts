export { DEFAULT_SCROLL_AREA_MARGIN_PX } from './helpers/metrics';

/** Default thickness (px) of both scrollbar tracks. */
export const DEFAULT_SCROLLBAR_TRACK_SIZE_PX = 8;

/** Default minimum thumb length (px) so a tiny proportional thumb stays grabbable. */
export const DEFAULT_SCROLLBAR_MIN_THUMB_SIZE_PX = 32;
